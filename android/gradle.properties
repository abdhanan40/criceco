# Heap sized for a 8 GB development machine running the Android emulator at
# the same time. The Flutter template default (-Xmx8G, 4G metaspace) exhausted
# system memory and killed the emulator (see docs/DEV_SETUP.md).
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=768m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
kotlin.daemon.jvmargs=-Xmx1G
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
